<h1>All found pets</h1>

<ul>
  <% @pets.each do |pet| %>
    <li>
      <%= link_to pet.name, pet_path(pet) %>
    </li>
  <% end %>
</ul>

<%= link_to "You found a pet?", new_pet_path, class: 'btn btn-primary' %>
